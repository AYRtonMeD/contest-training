#include <iostream>
using namespace std;

int fib (long long int num, int b){

}

int main(){
    long long int num;
    int base;

    while (cin >> num >> base && (num != 0 || base != 0)){
        fib ()
    }
    return 0;
}

