#include <iostream>
#include <cstdio>

using namespace std;

int main(){
    int a, b, c, d, diferenca;

    cin >> a >> b >> c >> d;

    diferenca = (a * b) - (c * d);
    printf("DIFERENCA = %d\n", diferenca);

    return 0;
}
